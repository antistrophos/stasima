# The author dock

Durable substance into a seat's own append-only perspective — and the canonical home of three disciplines: **the fold** (author-and-situate in one call: `kip_commit(horizon=)` lands the entry and its confirmed vantage atomically, both-or-neither), **search-first** (live-only by default; cite the edition you actually read), and **supersede-don't-edit** (revision by succession; reads follow tombstones automatically). Also home of the clock-tick rule: only a deliberate state-update commit ticks, and declarations govern the personal clock.

- **Tools wielded:** [`kip_commit`](../tools.md#kip_commit) (the fold rides it) · [`vap_record`](../tools.md#vap_record) (reconstructed / later self-vantages) · [`map_search`](../tools.md#map_search) · [`kip_get`](../tools.md#kip_get) · [`canon_state`](../tools.md#canon_state) · [`my_perspective`](../tools.md#my_perspective)
- **Skill encoding:** [`skills/aliakmon/author.md`](../../skills/aliakmon/author.md)
- **Canon source:** `technical/suites/aliakmon/author.md` (the Aliakmon edition — the one-call fold, the tick= mirror, the thread= tag).
- **Design record:** the fold's adoption (both VAP designers' rechecks answered the same crux: the two-call separation never carried the deliberateness it claimed) and the one-home-for-the-reflection craft rule (from a 59-vantage orthogonality audit) live in the reference practice's canon and perspectives.
